version: 1.0
runtime: python3
build:
  commands:
    pre-build:
      - echo "Installing dependencies..."
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.9
  command: python streamlit_app.py
  network:
    port: 8080
  environment:
    APPRUNNER: "TRUE"
